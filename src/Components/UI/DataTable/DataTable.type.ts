export interface ICompanyStockInfo {
  code: string;
  company: string;
  lastPrice: string;
  changePercent: string;
  openPrice: string;
  minPrice: string;
  maxPrice: string;
  avgPrice: string;
  volume: string;
  time: string;
}
